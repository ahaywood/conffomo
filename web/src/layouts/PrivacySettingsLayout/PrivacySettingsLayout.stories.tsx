import type { Meta, StoryObj } from '@storybook/react'

import PrivacySettingLayout from './PrivacySettingsLayout'

const meta: Meta<typeof PrivacySettingLayout> = {
  component: PrivacySettingLayout,
}

export default meta

type Story = StoryObj<typeof PrivacySettingLayout>

export const Primary: Story = {}
